node_modules/
dist/
*.db
*.db-wal
*.db-shm
.env
.env.*
!.env.example
!*/.env.example
*.log
*.docx
# Working documents that live beside the code but are not part of it:
# the cost model and the project tracker. Both are edited in Word/Excel,
# so tracking them would put an opaque binary blob in the history on every
# status change without ever producing a readable diff.
*.xlsx
.DS_Store
Thumbs.db
/Vuka Uzenzele - Cost Model.html

# Gijima IntelliSource — the employer project, not this one.
# It was committed here and this repository is public, which published a
# business case, a PRD, an architecture and a procurement planning review that
# were never ours to publish. Untracked (files kept on disk) so it stops being
# part of this project, in both senses.
IntelliSource/
